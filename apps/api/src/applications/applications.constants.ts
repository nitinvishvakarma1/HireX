/** Domain constants for the applications feature (Rulebook §4: no magic numbers). */

/** Inclusive lower bound of an explainable match score. */
export const MATCH_SCORE_MIN = 0;
/** Inclusive upper bound of an explainable match score. */
export const MATCH_SCORE_MAX = 100;
